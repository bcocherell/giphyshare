{{> navs/navBar }}
<h2 class="text-secondary text-center">{{dataValues.title}}</h2>
<div class="row">
    <div class="col-12 text-center">
        {{> posts/post singlePost=true}}
    </div>
    <h3 class="text-secondary text-left">{{dataValues.comment}}</h3>
</div>

{{> comments/commentSection}}