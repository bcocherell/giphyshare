{{> navs/navBar user='active'}} 
<div class="row justify-content-between">
    {{#each posts}}
        <div class="col-lg-3 col-md-6">
            <a href={{postUrl}}>
                {{> posts/post imgUrl=imgUrl }}
            </a>
        </div>
    {{/each}}
</div>